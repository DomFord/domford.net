import type { SignProgressFn } from '../types/progress';
export declare const execute: <T, Step>({ fn, step, onProgress }: {
    fn: () => Promise<T>;
    step: Step;
    onProgress?: SignProgressFn<Step>;
}) => Promise<T>;
